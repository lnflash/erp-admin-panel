name: CI

on:
  pull_request:
    branches: [main, feat/cashout-requests-page]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pre-commit:
    name: pre-commit (changed files)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          # Full history so the PR base/head commits are available for the diff.
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install pre-commit
        run: pip install pre-commit

      - name: Run pre-commit on files changed in this PR
        run: |
          pre-commit run \
            --from-ref "${{ github.event.pull_request.base.sha }}" \
            --to-ref "${{ github.event.pull_request.head.sha }}" \
            --show-diff-on-failure \
            --color=always

  pytest:
    name: pytest (admin_panel/tests)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      # The suite deliberately stubs frappe (see the test module docstrings),
      # so it needs no bench, no site and no database — pytest + requests is
      # the whole dependency set.
      - name: Install test dependencies
        run: pip install pytest requests

      - name: Run the test suite
        run: python -m pytest admin_panel/tests -q
